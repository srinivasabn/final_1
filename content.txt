1.
git init
ls -a
git config --global user.name "sri"
git config --global user.email sri@email.com
file create,add,commit,log
modify file ,status,add,commit ,log

2.
goto feature branch
create file,add,commit
move to main branch
modify same file,add,commit
git merge feature--conflict
git mergetool
git log --oneline
git branch -d feature

3.
create repo 
clone in bash
create file,add,commit
git push -u origin main
create file in repo
git pull origin main

4.
same upto 4 steps above
move to feature branch
moodify same file,add,commit,push feature
pull reguest
git branch -d feature
git push origin --delete feature

5.
clone repo
git tag v1.0
git tag -a v1.2 -m "hii"
git tag
git show v1.2
git push origin v1.0
git push --tags
git tag -d v1.0
git push origin -d v1.0

6.
mvn archetype:generate
cd sample_app
mvn package
cd target { snap file }
or { mvn clean 
mvn package }
{mvn site
target-site-index.html}
java -cp sampl_app snap.jar com.bnmit.App

7.
create a java file in git(hii)
add git link,credentials
build steps- javac BankService.java
java BankService
post build-artifacts
Files to archive-*.class
build, show output

8.
old bits,git url,credential,main
poll scm-5 stars
command javac BankService.java
java BankService
build
change code
wait for build

9.
open jenkins
pipeline
code 
pipeline {
    agent any // Run on any available Jenkins agent
    stages {
        stage('Build') {
            steps {
                echo 'Compiling the application...'
                // sh './build.sh' (for Linux) or bat 'build.bat' (for Windows)
            }
        }
        stage('Test') {
            steps {
                echo 'Running unit tests...'
                // sh 'mvn test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to production...'
            }
        }
    }
}
build,op

10.
download pipeline stageview
add error 
build,op
fix error
build,op
